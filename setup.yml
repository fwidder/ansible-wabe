---
- name: Setup Docker-Server
  hosts: localhost
  remote_user: root

  tasks:

  - name: Hello Server
    debug:
      msg: "Hello World!" 
      
  - name: Erstelle eine Datei
    file:
      path: /tmp/super_wichtige_devk_datei
      state: touch
